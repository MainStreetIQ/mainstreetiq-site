# msiq-site — Operating Rules

Promoted from Scott's session memory (2026-07-06, agent-native plan P1). This file is CANONICAL for site mechanics; the memory files are one-line pointers back here. Composes with `~/MSIQ/CLAUDE.md` and `~/.claude/CLAUDE.md`.

## Positioning architecture (Scott, 2026-07-23)

Governing brief for the site's two-offer structure. Overrides the earlier "site = hygiene not acquisition" framing.

- **AI Discoverability is the growth engine / lead generator, and it is a BROADER offer with wine as the beachhead.** Value prop: surface ALL the touchpoints a prospect may hit; when they are optimized and managed, the client sees more prospects at their door. The horizontal "AI Discoverability" brand is the front door; wine (WCIR / Winery Visibility Snapshot, SB + SLO) is the flagship proof point / first vertical, NOT the whole offer. When building the discoverability front door, it is a horizontal landing page with wine as the proof section — do NOT scope the nav/front-door to "MSIQ Wine".
- **Homepage: CFO-led hero, Discoverability as a COMPACT SECONDARY mention + a primary-nav peer.** Not a discoverability-led hero, not a two-door split. Keep the CFO hero (protects CFO search equity); Discoverability enters below it as a compact band and as a nav item. The nav "Discoverability" peer points at the horizontal discoverability landing page, not wineries.html. **AMENDED 2026-07-24 (Scott):** the homepage is *primarily CFO services with a mention of AI Discoverability*. The band was compacted (label + h2 + one paragraph + CTA; the 3-stat bar was removed) and the DDMA "Four readings, one engine" section was REMOVED from the homepage entirely, because DDMA is discoverability work, not the homepage's CFO spine. See the Tier 3 note below.
- **Fractional CFO is the deepen-and-retain relationship behind the discoverability front door.**
- **ICP = under $50MM, CEILING ONLY, no floor.** Floor phrasings ($5M-$50M, $10M-$100M, "scaling past $X", "between $XM and $50M") are cleaned to "under $50M" sitewide (shipped 2026-07-23, commits 931cddd + 0181253). KEEP: form revenue-dropdown bands, the Watch Gang $18M->$40M+ case study, and analytical figures (DTC margin-squeeze onset range, Amazon-halo >$5MM) — those are content/proof, not ICP-floor claims.
- **Locked verticals = ecommerce, wine, wellness, elective medicine** (aesthetics ⊂ elective medicine). locations.html hub blurbs realigned to these (shipped 2026-07-23). Do not reintroduce PE-backed / SaaS / biotech / apparel / entertainment / precision-manufacturing / franchises / medical-devices framing.
- **NAP phone: do NOT publish the phone number (805-270-3323) anywhere on the site (Scott, 2026-07-23).** Do not add `telephone` to any JSON-LD (`Organization`/`ContactPoint`) or to contact copy. This OVERRIDES plan fix §4.1 (which had proposed surfacing it for GEO). Email is fine; phone stays off.
- **Ecommerce / DTC is a LEAN-IN vertical (Scott, 2026-07-23).** It is where Scott has the bulk of his operating experience (Watch Gang first-finance-hire $18M→$40M+; DTC clients e.g. Skin Gym, Pig Brig, The Fidget Game). Fractional-CFO-for-ecommerce is a real, current offering. **The canonical ecommerce vertical page ALREADY EXISTS as `ecommerce.html` (`/ecommerce`, 475 lines, indexed, in sitemap.xml) — it is the built-out page, not a stub.** The `fractional-cfo-dtc-ecommerce.html` file is a redirect stub that correctly consolidates SEO into `/ecommerce`; **leave it as a redirect — do NOT "build it out," which would duplicate `/ecommerce` and split ranking signal** (2026-07-23 correction: an earlier baton mistakenly described this stub as a page to build; `ecommerce.html` is the real page). The lean-in work is strengthening `ecommerce.html` and ecommerce proof across the site, not creating a second ecommerce page.
- **Still blocked:** distinct Zoho Lead_Source per lane (CFO / Wine / Referral) waits on the picklist rename (`project_zoho_lead_source_rename`). The CFO "Book an Intro Call" -> Calendly path now DOES create a Zoho lead (web-to-lead form on intro-call.html; see the Tier 3 item-4 note below). Until the per-lane rename lands it uses the generic `Lead Source = "Calendly Booking"` (an existing valid picklist option).

### Two-lane architecture (Scott, 2026-07-26)

Four decisions that unblocked the two-lane build, plus one standing geographic rule. Baton: `~/Mainstreet/_brain/handoffs/2026-07-26-cfo-hub-ai-visibility-built.md`.

- **CFO hub is a NEW `/fractional-cfo` page** (`fractional-cfo.html`), built as the peer to `/discoverability`. ~~`our-services.html` STAYS INTACT as the blended "how we work" page~~ **AMENDED 2026-08-03 (Scott): `our-services.html` COLLAPSES to a "How We Work" page — see § Two-lane IA below.** Its URL must still keep working (every blog post carries its nav link). The hub is national in schema (`State: California` + `Country: United States`), not county-scoped; the 6 `fractional-cfo-<county>` pages child off it. It links out to `/glossary/fractional-cfo` (definitional intent) to avoid cannibalizing it; the glossary page does NOT yet link back up, which is worth adding.
- **Discoverability local pages are named `ai-visibility-<county>`**, not `discoverability-<county>`, because that is how buyers search (precedent: `ai-visibility-tourism.html`). The hub stays `/discoverability`; the hub/child stem mismatch is ACCEPTED, do not "fix" it by renaming the hub (44 root pages and 35 blog posts link `/discoverability`).
- **NO city pages.** The 56 cities already enumerated in the county pages' `City` JSON-LD stay as schema plus body text. Revisit only once both lanes are symmetric. Rationale: 56 to 112 templated near-duplicate pages is real doorway-page risk. **SCOPE CLARIFIED 2026-08-03 (Scott): this rule governs the CFO and discoverability COUNTY lanes, where a city page would be a near-duplicate of its county parent with the place name swapped. It does NOT govern the wine-town data pages (`/wine-country/<town>`), which are a different artifact: each carries per-town MEASUREMENTS of named businesses from the CCIQ DB, so no two pages share substantive content. Do not delete them as doorway pages. If a town page ever ships without town-specific data, that page IS a doorway page and the rule applies to it.**
- **DDMA re-home off the remaining 12 pages is a SEPARATE scoped pass.** See the blocker note under Gate adjudications below.

- **HOME AND SERVICES PAGES MUST NOT ISOLATE MSIQ TO THE CENTRAL COAST (Scott, 2026-07-26).** Individual county pages, community pages, and the About section MAY speak to location. The practice serves the US remotely; the six counties are outbound and on-site CONCENTRATION, not client-acceptance scope (canonical-facts § Geographic scope, amended 2026-07-03). Executed 2026-07-26: `index.html` `areaServed` had listed the six counties and nothing broader, so `Country: United States` + `State: California` were added above them and `geo.placename` became "California, United States"; `our-services.html` `Place: "California Central Coast"` became `Country: United States` with the same `geo.placename` change; `fractional-cfo.html` carried it in VISIBLE copy and was rewritten to lead with national reach (it now contains zero instances of "Central Coast"). Both index and our-services edits were schema/meta ONLY, no visible copy, so no new prose entered the gate.

- **The `ai-visibility-<county>` pages are GENERATED. Edit `_scripts/build_ai_visibility_pages.py` and regenerate; never hand-edit the 6 HTML files.** Idempotent, supports `--dry-run`. Two constraints are baked into that script and must not be "fixed" into symmetry:
  1. **PROOF SCOPE.** WCIR covers Santa Barbara and San Luis Obispo only, and the county stat is SANTA BARBARA-scoped. Three variants: `measured` (SB carries the stat), `covered` (SLO cites the WCIR but asserts NO SLO-specific count, because none is published), `method` (Ventura / LA / Orange / San Diego carry NO local stat and say so plainly). brand-guardian reviewed the San Diego refusal and judged it reads as strength, not apology: do not soften it. Guard: `for f in ai-visibility-*.html; do echo "$f $(grep -c '71 of 171' $f)"; done` must be non-zero on santa-barbara ONLY.
     **REFRESHED TO Q2 2026 on 2026-07-30. The old guard string was `86 of 175`; it now appears nowhere on the site.** Two things changed and a future session must not "correct" either back:
     - **The stat flipped to the INVISIBLE side.** Q1 was "86 of 175 AI-visible" (49%, a minority). Q2 measured SB at **100 of 171 cited**, which is a MAJORITY, so the visible-side framing no longer supports the word "only" and reads as good news. The same measurement stated as the invisible side is **71 of 171** for SB.
     - **Scope split by page (Scott, 2026-07-30).** The four hand-maintained pages (`index`, `discoverability`, `wineries`, `winery-visibility-snapshot`) carry the REGION-wide figure, **228 of 423** Central Coast tasting rooms named by no AI assistant, which matches the Q2 Central Coast edition's own "roughly half-visible" headline and covers both counties the WCIR now measures. `ai-visibility-santa-barbara.html` stays **SB-scoped at 71 of 171**, because that page is county-targeted and a region figure would break PROOF SCOPE.
     - **Open decision, deliberately NOT executed:** SLO now HAS a published count (95 of 252 cited, 157 of 252 not named), so `ai-visibility-san-luis-obispo.html` could move from the `covered` variant to `measured`. That is a scope change the rule above does not contemplate and it needs Scott's call. The `covered` variant's premise ("no published SLO-specific count") is now false; leaving it is conservative, not correct.
     - Q2 figures, derived via `CCIQ/generate_central_coast_report.ai_stats()` and confirmed against the rendered PDF: **SB 100 of 171 cited (58%), SLO 95 of 252 (38%), region 195 of 423 cited / 228 of 423 not named.**
  2. **GEOGRAPHIC HONESTY.** Only SLO and Santa Barbara are ON the Central Coast (`home_turf: True`); the other four are SERVED FROM it. An earlier draft hardcoded "the practice is based here on the Central Coast" onto all six, which is false for San Diego, LA, Orange, and Ventura, and shipped a "Local / Based on the Central Coast" hero stat alongside it. The CFO-lane sibling already had this right. Guard: `for f in ai-visibility-*.html; do printf "%s here=%s from=%s\n" "$f" "$(grep -c 'based here on the Central Coast' $f)" "$(grep -c "from California's Central Coast" $f)"; done` must be 2 and 4.

### Two-lane IA (Scott, 2026-08-03) — SCOPED, NOT YET BUILT

Scott's ruling on the site restructure. Plan: `~/.claude/plans/snoopy-moseying-feather.md`.
**Nothing below is built yet except the three rule amendments themselves.**

Diagnosis that drove it, all verified 2026-08-03:
- **`/fractional-cfo` had ZERO inbound internal links and was not in the nav.** Built 2026-07-26 as
  the peer to `/discoverability`, then never linked from anywhere. It also repeats three `index.html`
  H2s verbatim ("The RAID model", "Two ways that work", "A CFO lens, not an agency lens").
- **Three pages collide on the same head term:** `/` ("Fractional CFO for Ecommerce + Owner-Operated
  Businesses"), `/our-services` ("Fractional CFO Services | How We Work"), `/fractional-cfo`
  ("Fractional CFO Services for Owner-Operated Businesses").
- **`/our-services` states the same SKU list three times** — "The Offerings Behind Each Reading"
  (`:188-267`), "Each Step Builds on the Last" (`:269-337`), "Three Places You Can Be" (`:339-417`).
- **Monitor and Engage have no SKU page**, while Discover/Diagnose/Manage do. Monitor is the SKU the
  quarterly WCIR email exists to sell.
- **`llms.txt` omits the entire discoverability lane** (`/discoverability`, `/wine-country-intelligence`,
  `/audit`, `/winery-visibility-snapshot`, `/manage`, `/financial-stack`, `/partners`, and every
  `ai-visibility-<county>` page except tourism) and claims "31 posts" against 48 on disk.
- **0 of 48 blog posts link to `/fractional-cfo` or `/audit`.** Not an oversight in judgment: the
  2026-07-23 in-body link pass recorded "there is NO bare `/fractional-cfo` page (it 404s)", which was
  true that day. The hub shipped three days later and the pass was never re-run.

The rulings:
- **Nav becomes two lanes:** `Fractional CFO` (`/fractional-cfo`) | `AI Discoverability`
  (`/discoverability`) | About | Our Work | Blog | Partners | Contact | [Book an Intro Call].
  `/our-services` drops OUT of the nav, keeps its URL, and stays linked from both hubs.
- **`/our-services` collapses to "How We Work."** Keeps the practice narrative, a brief "Four
  Readings, One Engine", "Three Places You Can Be", "What You Get". The two catalog sections MOVE to
  the SKU pages — move the copy, do not rewrite it. Retitle off "Fractional CFO Services" to end the
  three-way collision.
- **Build `/monitor` and `/engage`,** modeled on `manage.html` (the existing gated SKU-page pattern).
  Both CROSS-VERTICAL with wine as a proof section. Monitor $400/mo, $4,000/yr and Monitor Plus
  $600/mo, $6,000/yr are cleared for public surfaces; Engage carries no price ("scoped on an Intro
  Call"), same as Manage. **Grow stays unnamed.**
- **Build `/wine-country/<town>` x10** (Los Olivos, Solvang, Santa Ynez, Buellton, Lompoc, Los Alamos,
  Santa Maria, Paso Robles, Templeton, Edna Valley) as the demand-creation surface. **GENERATED, not
  hand-written** — follow `_scripts/build_ai_visibility_pages.py`. Figures come from the CCIQ queries
  that build the PDFs, never retyped off `wine-country-intelligence.html`. Named wineries are
  positively framed only. PROOF SCOPE applies per town: a town asserts only what its data supports,
  and says so plainly where the count is too small.
- **These town pages are also the backlink plan.** The WCIR is the only asset that earns links
  (original research naming 423 businesses), and it earns none today because it has no linkable
  public surface: `/reports/` is robots-disallowed and `/wcir/<quarter>` is deliberately unlisted.

**The split-URL guard still binds.** Town pages link UP to `/wine-country-intelligence` only — never
to the delivery page, never to a `/reports/*.pdf` (the PDF path contains the quarter slug).

### WCIR quarterly delivery pages + DDMA completion (Scott, 2026-07-30)

Three rulings from the session that put the Q2 Central Coast report on the web.

- **SPLIT URL ARCHITECTURE for the quarterly report. Two pages, two jobs, and they must not be merged.**
  1. **`/wine-country-intelligence` stays PUBLIC and INDEXED.** It is the evergreen hub: what the report
     is, the six-dimension method, current-quarter headline findings, the services menu, the booking path.
     It keeps its sitemap entry and all inbound links (12 files reference it). It is the SEO and
     AI-citation asset. Refreshed off the stale "Q1 2026 Santa Barbara" framing on 2026-07-30.
  2. **`/wcir/<quarter>-<edition>` is the UNLISTED per-quarter delivery page** (first one:
     `/wcir/q2-2026-central-coast`). `noindex, nofollow, noarchive`, absent from `sitemap.xml` and
     `llms.txt`, and **ZERO inbound links from any page**. It is the exact link in the prospecting
     email. Each quarter gets its OWN dated URL so a past send never breaks and never silently starts
     showing different data.
  **Guard before any commit that touches these:**
  `grep -rl "<quarter-slug>" --include="*.html" --include="*.xml" --include="*.txt" .` must return
  ONLY the delivery page itself. Any other hit means the unlisted page has been linked.
- **robots.txt: `/wcir/` and `/reports/` need OPPOSITE treatment. Do not "fix" this into symmetry.**
  `/wcir/` is deliberately NOT disallowed, because a crawler must be ALLOWED to fetch a page in order
  to read its `noindex`. Disallowing it would block the crawl, leave the noindex unread, and let Google
  index the bare URL anyway. `/reports/` IS disallowed, because a PDF cannot carry a meta robots tag
  and GitHub Pages cannot set `X-Robots-Tag`. **Neither is access control.** Both are advisory, bind
  only compliant crawlers, and do not stop forwarding. GitHub Pages has no authentication; anyone
  holding the link can read either. Say that plainly to Scott rather than implying the page is private.
- **DDMA cleanup scope = wine lane + `our-services.html` ONLY.** The 6 `fractional-cfo-*` pages and
  `about` / `ecommerce` / `wellness` / `aesthetics` were explicitly left alone.
  **This did NOT require amending `adjudications.yaml`,** and a future session should not think it did:
  the blocker recorded below guards a DDMA *re-home / strip* pass. This was the opposite. It KEEPS DDMA
  where the 2026-07-19 ruling puts it (site-wide engine spine, on `our-services`) and COMPLETES it.
  "RECONCILE, do not relocate" was satisfied, not violated.
  - `our-services.html`: "The Offerings Behind Each Reading" said **"Three productized verbs"** and
    covered only 01 Discover / 02 Diagnose / 03 Monitor, with Advise demoted to a bridge paragraph.
    Now four, with a real `04 | Advise` block.
  - `wineries.html`: a fifth **"Ad hoc"** card sat as a peer to the four readings and broke the story.
    Moved out of the `problem-list` into a marked "Outside the four readings: project work" addendum.
- **Under Advise, name Engage and Manage publicly. Grow stays unnamed** per canonical-facts
  (never displayed publicly). Neither Engage nor Manage carries a price on a public page; both are
  "scoped on an Intro Call". Wine **Monitor $400/mo, $4,000/yr and Monitor Plus $600/mo, $6,000/yr
  ARE permitted** on public surfaces per canonical-facts.
- **`wineries.html` is CORRECT about Monitor and the county PDFs are WRONG. Do not propagate the PDF.**
  The site says Monitor carries **five** drift-watch alerts and puts **peer movement under Monitor
  Plus**, matching `stripe_skus.md:71` and `03_monitor_subscription_scorecard.md:70-88`. The SB and SLO
  county editions claim "seven Drift Watch alerts" and attribute peer movement to Monitor. That is an
  open defect in the PDFs (baton open thread 1), not a site error to "fix" into agreement.

### Tier 3 build decisions (Scott, 2026-07-23, execution session)

- **Homepage Discoverability treatment = Option A: a NEW dedicated band, high on the page.** Keep the CFO hero ("Finance teams break as you grow...") untouched. Insert a NEW, visually-distinct AI-Discoverability band: demand-side headline, the horizontal value prop (surface every prospect touchpoint, optimize + manage, more prospects at your door), CTA -> `/discoverability`. ~~LEAVE the existing "Four readings, one engine" (DDMA) section where it is~~ **SUPERSEDED 2026-07-24: see the DDMA-removal note below.** The original rejection of Option B (reframe DDMA *as* the band) still stands: the band and DDMA are not the same thing and the band copy must not just restate DDMA's "Monitor / AI-search visibility" line.
- **Homepage DDMA REMOVED + band compacted, SHIPPED 2026-07-24 (session 7).** Scott: *"the main page should be primarily about the CFO services and a mention of the AI Discoverability. Discover, Diagnose, Monitor, Advise are a part of the discoverability work, they should be moved from the main page. Four readings one engine is a discoverability concept."* This SUPERSEDES both the 2026-07-23 "leave DDMA where it is" decision above and the site-placement half of the 2026-07-19 RAID-vs-DDMA adjudication. Executed on `index.html` ONLY:
  1. Deleted the whole `<!-- ===== THE INSTRUMENT SET ===== -->` section (h2 "Four readings, one engine" + the four Discover/Diagnose/Monitor/Advise cards + its closing bridge paragraph). Nothing was lost: the canonical DDMA treatment lives on `our-services.html` ("Four Readings, One Engine" + the deeper "The Offerings Behind Each Reading" breakout), and the deleted bridge copy was already duplicated by the band's "front door to the practice" line and by "Two ways that work"'s own Intro Call closer.
  2. Compacted the Discoverability band to label + h2 + one paragraph + CTA; removed the 3-stat bar (`86 of 175` / `4` / `Monthly`). That was the homepage's only load-bearing WCIR data claim; it still lives on `/discoverability` where the proof section belongs.
  3. Rewrote the "What does Main Street IQ actually do?" answer around RAID + Engage/Manage with no DDMA enumeration. **It exists TWICE (visible FAQ + FAQPage JSON-LD) and must stay byte-identical**, verified programmatically, not by eye.
  4. Dropped `"Discover","Diagnose","Monitor","Advise"` from the ProfessionalService JSON-LD `serviceType`.
  5. Fixed the one orphan the deletion created: "after a Diagnose-driven repositioning" -> "after a repositioning driven by what the read surfaces". **`Monitor Plus` elsewhere on the page was deliberately KEPT**: it is a real purchasable SKU, not a DDMA framework reference. Same for the `86 of 175` stat inside the wineries card in "Where we go deepest" (in-context vertical proof, not the band's stat bar).
  Resulting section order: hero -> problem -> RAID -> CFO lens -> **compact Discoverability band** -> Two ways that work -> ... **NOT DONE (out of scope, needs a separate Scott decision):** DDMA cards still appear on 12 other pages (`our-services`, `about`, `ecommerce`, `wellness`, `aesthetics`, `wineries`, 6 `fractional-cfo-*` location pages). Scott's "DDMA is discoverability work" framing logically implies re-homing it there too; do not do that unilaterally.
- **New `/discoverability.html` horizontal hub — SHIPPED 2026-07-23 (session 5, commit 5563b94).** Structure built as specced: hero (horizontal value prop) -> what we read (4 layers: classical search / answer engines / generative AI chat / reputation-listings) -> surface-optimize-manage -> PROOF section = Wine (WCIR, 86/175 SB stat, SB+SLO, linked to /wineries + /winery-visibility-snapshot) -> your-vertical-next (ecommerce / wellness / elective medicine) -> FAQ (4 Q&A + matching FAQPage schema) -> CTA. Wine is a proof SECTION, not the whole page. In sitemap.xml at priority 0.9. Gate: aggregate 99, all six checkers PASS, two clean rounds (compliance round-1 flagged two FTC-substantiation near-misses — a multi-quarter "gap widens each quarter" trend claim and a "prove it works" FAQ framing; both softened to single-snapshot / measurement framing before ship).
- **Nav "Discoverability" peer — SHIPPED 2026-07-23 (session 5, commit 7bdbfc9).** Added `<a href="/discoverability">Discoverability</a>` between Services and About across 120 pages (nav is inlined per-page, not a shared include; anchor = the Services nav link, which uniquely precedes About — the footer "Company" column has About->Services so it was not touched). Verified one nav link per page, zero footer contamination. our-services.html keeps `class="active"` on Services. Skipped (correctly): redirect stubs (fractional-cfo-dtc-ecommerce, fit-call*), card.html (no nav), `_examples/` (Jekyll underscore-excluded).
- **Homepage Option A band — SHIPPED 2026-07-23 (session 5, commit 7bdbfc9).** Navy+sky band inserted immediately after the CFO hero (before "Finance is the function that breaks quietly"): label "AI Discoverability | the growth engine", h2 "Your buyers are asking AI who to buy from" (varied from the page H1 to avoid echo), horizontal value prop, 3-stat bar (86 of 175 / 4 / Monthly), CTA -> /discoverability. DDMA "Four readings" section left in place. Gate: aggregate 99, all six PASS. **REPOSITIONED 2026-07-24 (session 6, commit b386d24):** Scott wanted it off the #2 slot. Moved from directly-after-hero to AFTER the "A CFO lens, not an agency lens" section, so the CFO story opens first (hero -> problem -> RAID -> CFO lens -> Discoverability band -> instrument set). **Band COMPACTED 2026-07-24 (session 7) — the 3-stat bar was removed; see the DDMA-removal note below.** Kept the bold-navy treatment; placed after the white CFO-lens section specifically so two `bg-navy` sections (RAID + band) don't abut — only `bg-navy` and `bg-light` section treatments exist, so band-immediately-after-RAID would merge into one midnight slab. Still a strong-secondary band + nav peer.
- **Intro Call -> Zoho capture — SHIPPED (item 4, closed 2026-07-23 session 6, commit 08d84ff).** The baton mislabeled this OPEN; the form was already built and live. Approach A end-to-end: `intro-call.html` carries a Zoho `WebToLeadForm` (form ID `xnQsjsdp`) capturing name/email/company/vertical/revenue/context -> creates a Lead -> `returnURL` redirects to `/intro-call-confirm` (noindex) which embeds the Calendly inline widget. Vertical is NOT posted to Zoho's strict Industry picklist by design; it rides in the Description prefix. What session 6 actually fixed: the live-picklist verification the baton demanded had never been done, and it caught two silently-blanked fields — `Lead Source="MSIQ Website"` (not a valid `Lead_Source` option) and revenue bands `Under $1M`/`$1M-$5M`/`Over $50M` (not valid `Revenue_Band` options). Verified via live Zoho Leads getFields and remapped: Lead Source -> `Calendly Booking`; revenue option `value`s -> the live `Revenue_Band` picklist (`Under $5M / $5M-$10M / $10M-$25M / $25M-$50M / $50M-$100M`), with the granular label kept in the Description so sub-$1M vs $1M-$5M qualification signal survives. `Lead Status="Pre-Qualified"` was already valid. Visible copy unchanged, so gated by lint + browser-verify + live-Zoho conformance (no new prose -> no 4-checker content gate). **Lesson (verify-handoff-vs-authority): "picklist-verified" in a baton is not proof it was done — re-run getFields against the values the form actually POSTs.**
- **DONE this session (Tier 1b, all pushed to main):** ICP floor->ceiling harmonization (931cddd/0181253, prior session); locations realign (0181253); sitemap pillars (858d5d4); social-card backfill on 4 indexable pages (81376f1); raid-framework BreadcrumbList (5305914, FAQPage intentionally NOT added, no FAQ content); 6 blog in-body pillar links (14a0a18); vs-cpa 3-role comparison converted to a semantic `<table>` (49212e6, FAQ "when a CPA suffices" language kept as analytical). **Verified already-done (no action needed):** ecommerce vertical page (`ecommerce.html` is the real `/ecommerce`, the dtc-ecommerce stub stays a redirect), fit-call dead pages (already noindex redirect stubs to /intro-call), glossary DefinedTerm (all 16 term pages + DefinedTermSet hub already present).
- **Blog in-body internal-link pass — DONE 2026-07-23 (session 5, commit 87ad954).** 37 contextual links added across 25 posts (on top of the earlier 6), first natural in-body term mention -> glossary definition page. **Correction to the prior baton: there is NO bare `/fractional-cfo` page (it 404s) — the real target is `/glossary/fractional-cfo`** (a glossary term page). Verified live-200 glossary targets used: `/glossary/{fractional-cfo, cac-ltv-ratio, unit-economics, contribution-margin, cash-conversion-cycle, answer-engine-optimization, generative-engine-optimization, ai-visibility, schema-markup}`. Blog in-body links use PLAIN markup (`<a href="/glossary/x">term</a>`, no inline style), unlike marketing pages. Executed via 4 flat general-purpose subagents (~8 posts each), edits verified on disk (diff + all targets curl-200), not from agent reports. Agents correctly skipped list-item / TL;DR-aside / heading / existing-anchor occurrences (the `<p>`-only rule). Note: a separate pre-existing lint family (em-dashes in the gtag / Calendly JS-comment boilerplate on every post) is out of scope and unfixed.

Full audit + tiered plan: `~/MSIQ/_reference/2026-07-23-msiq-site-coherence-plan.md` (§0 = this brief).

## Deploy & publish mechanics

- **Deploy is GitHub Pages, on push to `main`** (the `pages-build-deployment` Action). This repo is its OWN git repo (`MainStreetIQ/mainstreetiq-site`), separate from the parent `~/MSIQ` repo. It is NOT on Vercel — `vercel.json` (19 redirects) and `.vercelignore` are DEAD config, inert on Pages (adjudication pending, plan P5). Before asserting anything about deploy behavior, probe the live surface (`curl -sI` headers, a URL that should/shouldn't exist) — config-file presence is weak evidence and has already put a wrong platform claim into a committed doc.
- **A push that touches `.github/workflows/` needs `workflow` token scope, and no Claude session can grant it (learned 2026-08-03).** The rejection reads `refusing to allow an OAuth App to create or update workflow .github/workflows/scheduled-publish.yml without workflow scope`, which names the FILE and so reads like a repo or branch permission problem. It is not; it is a scope on the credential. **Do not retry the push, and do not try `gh auth setup-git`** (the helper is already `osxkeychain`; the helper is not the problem). **`gh auth refresh` cannot be run from a Claude session** either: it blocks on a TTY prompt that never arrives and dies at exit 144 when killed. Hand Scott exactly one line to paste in his own terminal, then keep working on anything that does not need the push: `gh auth refresh -h github.com -s workflow`. If only the workflow change is blocked and the rest must ship, keep the workflow edit as the TIP commit and push `HEAD~1:main`.
- **Draft privacy gate = Jekyll's underscore-directory exclusion.** `_scheduled/` and `_scripts/` stay off the live site ONLY because of the leading underscore. **NEVER add `.nojekyll`** (the common asset-fix reflex) — it would instantly publish every staged draft.
- **Scheduled publish:** `.github/workflows/scheduled-publish.yml` runs `_scripts/publish_scheduled.py` daily and stages exactly `git add blog/ llms.txt sitemap.xml` — the three files the script is capable of writing, and nothing else, so a stray working-tree file can never be swept into prod. A post change that touches any OTHER file must be committed separately or it won't ship with the post.
  **AMENDED 2026-08-03. `sitemap.xml` was NOT staged before that date, and the script did not write it at all.** The consequence was silent and total: nothing in the repo ever added a sitemap entry for a drip-published post, so all 12 posts the Action had ever published were absent from `sitemap.xml` (35 of 47 live posts listed), including all six "Found by the Machine" AI-visibility posts. A backfill alone would have re-broken on the next drip. The fix was structural, in three parts, and all three are load-bearing: (1) the script now owns per-series `<!-- ATC-SITEMAP-* -->` / `<!-- AIV-SITEMAP-* -->` marker pairs in `sitemap.xml`, exactly as it already owned the `*-GRID-*` and `*-LLMS-*` pairs; (2) the Action stages `sitemap.xml`; (3) the 12 missing entries were regenerated by running the script, not hand-written. **Do not "simplify" the workflow's `git add` back to two paths.**
- **The scheduler diverges `main` between sessions — ALWAYS `git fetch origin main` + rebase before any manual push.** The daily Action commits `blog/*.html` + `blog/index.html` + `llms.txt` + `sitemap.xml` (~16:0x UTC), so a manual push staged against a stale local `main` will be rejected as non-fast-forward. The recurring collision files are `llms.txt` and, since 2026-08-03, `sitemap.xml` (the scheduler regenerates blocks in both). If you're holding stale local copies (e.g. a deferred ICP rewrite), `git stash push -- llms.txt sitemap.xml` to clean the tree, rebase onto `origin/main`, push, and do NOT pop the stash over the scheduler's fresh files. Your top-level page edits never collide with `blog/`, so the rebase is conflict-free once those two are out of the working tree. (2026-07-21: free-sweep push rejected this way; fetch+stash+rebase resolved it.)
  **Note the asymmetry on `sitemap.xml`:** only the marker-delimited blocks are scheduler-owned. Hand-maintained entries (every non-drip page) sit outside them and are safe to edit directly, so a conflict there is a real conflict, not a regeneration artifact.

## Blog drip system

- Posts stage in `_scheduled/<series>/` with a per-series `manifest.json`; the daily Action promotes each into `blog/` on its slot date and rebuilds three marker-delimited blocks per series: the card grid in `blog/index.html` (`ATC-GRID-*`, `AIV-GRID-*`), the block in `llms.txt` (`*-LLMS-*`), and the `<url>` entries in `sitemap.xml` (`*-SITEMAP-*`, added 2026-08-03). Multi-series via the `SERIES` registry in the script. A NEW series needs: registry entry + `_scheduled/<key>/manifest.json` + post HTML + **all three** marker pairs in `blog/index.html`, `llms.txt`, and `sitemap.xml` — a missing pair is a hard `SystemExit`, not a silent skip, which is deliberate. Edit a pre-date post in its `_scheduled/` source; picked up on its cron run. Dry-run: `python _scripts/publish_scheduled.py --check` or `--date YYYY-MM-DD`.
- **The scheduler is self-reconciling, so a gap in a scheduler-owned block is repaired by RUNNING it, never by hand-writing the missing entries.** That is how the 12 missing sitemap entries were backfilled on 2026-08-03: add the markers, teach the script, run it. Hand-writing them would have produced the same bytes and left the next drip broken. Verify with a second run — it must report `No changes (already in sync)`.
- **Editing a scheduler-OWNED `llms.txt` marker block (`ATC-LLMS-*`, `AIV-LLMS-*`) is non-durable in `llms.txt` — fix the SOURCE.** The block text is regenerated from each series' `manifest.json` `llms_desc` field on every Action run, so a hand-edit inside the markers is overwritten the next day. To change a block line permanently (e.g. strip an em-dash), edit the post's `llms_desc` in `_scheduled/<key>/manifest.json`; the `card_desc` field feeds the `blog/index.html` grid separately. Only content OUTSIDE the marker pairs (header/positioning blockquote, Core Services, Case Studies, RAID, Contact, Service Areas) is hand-maintained and safe to edit directly. To VERIFY a manifest fix matches what the scheduler will regenerate: `cp llms.txt <scratch>/mine`, run `_scripts/publish_scheduled.py`, `diff` just the block (`sed -n '/ATC-LLMS-START/,/ATC-LLMS-END/p'` on both), then restore with `cp <scratch>/mine llms.txt` — **NEVER `git checkout -- llms.txt` while your own uncommitted edits live in that file; it silently clobbers them** (learned 2026-07-23, the deferred ICP rewrite). The scheduler run also dirties `blog/`; revert those with `git restore blog/` + `rm` any stray promoted post, since promotion is the Action's job, not a manual push's.
- Blog posts drafted via the blog-draft skill must NOT include a `## TL;DR` heading in `body_md` — render.py builds the styled aside from the separate `tldr` field; a body TL;DR renders twice.
- LinkedIn is manual (batch docs in `~/MSIQ/_reference/linkedin-*.md`, loaded into LinkedIn's native scheduler). Only the blog auto-publishes.

## Verification harness

- Browser-verify style work before commit with `~/MSIQ/.venv/bin/python` + Playwright headless Chromium on `file://` URLs; screenshot to scratchpad and Read the PNG; catch JS errors via `page.on('pageerror')`. Desktop 1440x900 + mobile 390x844 covers both breakpoints.
- **Subdirectory pages (e.g. `/glossary/*.html`) use ABSOLUTE asset paths (`/styles.css`, `/assets/...`) and render UNSTYLED over `file://`** (the leading `/` resolves to the filesystem root, not the repo). Verify those over a local HTTP server instead: `python3 -m http.server 8899 --directory .` then load `http://localhost:8899/<page>.html`. Note python's http.server has NO extensionless-URL resolution (hit `.html` locally), but GitHub Pages does (production links stay extensionless). Root-level pages use relative paths and still style over `file://`.
- **`:focus-visible` must be verified with a real keyboard Tab** (`page.keyboard.press('Tab')` loop until `document.activeElement` is the target). Programmatic `element.focus()` + getComputedStyle false-negatives (outline reads back as currentColor).

## Gate adjudications

Settled checker false positives for this site's pages live in `~/.claude/skills/pre-publish-gate/adjudications.yaml` (injected into every checker prompt by the orchestrators). Highlights an agent editing site copy must know:

- "Website Audit" / "Website Audit Pro" are CURRENT service names (the 2026-05-20 rename was reverted 2026-05-22). Never "fix" them to Customer Acquisition Audit.
- Nationwide language is fine: six counties = outbound concentration + on-site availability, NOT client acceptance (canonical-facts § Geographic scope, amended 2026-07-03).
- "U.S. Navy Veteran" is the canonical phrasing (Entity table); LinkedIn not displaying it is not a contradiction.
- "verticals" vs "practices" is a Scott decision, not an auto-fix — "verticals" is the established sitewide convention.
- **RAID and DDMA are NOT competing frameworks — do not "reconcile away" or retire either (Scott, 2026-07-19).** RAID Framework = the overall strategy for running a finance team, i.e. the CFO practice methodology, applies across verticals. DDMA (Discover / Diagnose / Monitor / Advise, the homepage/our-services "Four readings") = the intelligence engine's instrument set. They operate at different levels: RAID is the practice, DDMA is the engine. **Resolution (Scott, 2026-07-19, second pass): RECONCILE, do not relocate.** DDMA is the SITE-WIDE engine spine (index, our-services, about) and its sub-products (Website Audit, Buyer Alignment, FBD, Monitor / Monitor Plus) are CROSS-VERTICAL — NOT winery-only. Do not move DDMA "under wineries" or claim those products are winery-specific; only "Wine Discover" is winery-scoped. The only over-claim to fix was framing the four readings as "one practice"; that is now "four readings, one engine" (RAID = the practice that decides; DDMA = the engine that reads). The named D/D/M/A framework goes deepest in the winery WCIR context, but the readings and their products stay cross-vertical. Do not re-elevate "one practice" and do not re-scope DDMA to winery-only.
- "Fit Call" is KILLED from all public-facing surfaces (Scott, 2026-07-05) — site copy, metas, JSON-LD, FAQ schema, CTAs. Do not reintroduce it.
- **Do NOT advertise the Intro Call as "free" (Scott, 2026-07-20).** "Free" reads cheap and undercuts CFO-advisory positioning. Point CTAs at the action: book a call, send a message, ask a question. ~~"No pitch, no obligation" is fine (reassurance, not price).~~ **AMENDED 2026-08-04 (Scott): "no pitch" / "no strings" / "no obligation" reassurance framing is KILLED everywhere — site copy, blog CTAs, metas, JSON-LD/FAQ schema, reports, emails, outreach templates. Verbatim brief: "we are absolutely pitching our services for which we will charge; this wording is inaccurate and unhelpful." Known instances: intro-call.html hero "No pitch.", blog CTA "30 Minutes. No Pitch." (~45 blog/glossary/our-work pages), any "no obligation" in email/outreach skills. UNSETTLED boundary (do not assume): whether lead-magnet "free" (the 7/21 KEEP below) also falls; the kill definitely covers no-pitch/no-strings/no-obligation. Amend `adjudications.yaml` BEFORE gating the sweep or checkers will enforce the old KEEP.** SURGICAL: remove only the "free [call / consultation / Intro Call]" cheapness sense from copy, metas, JSON-LD, FAQ. KEEP "free cash flow" (finance term, actually 0 hits site-wide), "free up hours/time", "risk-free", legal/terms + legal/privacy boilerplate, and competitor-comparison "free graders / free AEO check" (audit.html). **Two boundaries settled 2026-07-21:** (1) **lead-magnet "free" is a KEEP** — winery-visibility-snapshot ("Free Winery Visibility Snapshot"), the *-assessment pages ("Free, delivered"), and the WCIR "Free sample" / "free 10-page summary" are free deliverables where "free" is a value-prop, not a cheap sales call; do not strip. (2) **blog / glossary / our-work case-study "free call" CTAs are a SEPARATE deferred sweep**, NOT part of this one (~45 pages still say "Book a free 30-minute call"). Homepage + site-wide service/location sweep DONE 2026-07-21 (19 files: token `free 30-minute`->`30-minute`, hero stat `<h3>Free</h3>`->`30 min`, section-label "Free Intro Call"->"Intro Call", "30 minutes. Free."->"30 minutes.", intro-call.html price-card "Free" pill dropped keeping $0). Blog/glossary/our-work sweep still pending.
- **BLOCKER for the DDMA re-home pass: this file's own RAID/DDMA adjudication must be amended FIRST.** `adjudications.yaml` still carries the 2026-07-19 ruling that DDMA is the SITE-WIDE engine spine (index, our-services, about) and that the resolution is "RECONCILE, do not relocate." Baton 7's homepage removal superseded only the homepage half. A 12-page strip contradicts the rest, and every checker is briefed on that rule, so it will be blocked. Amend the adjudication before the pass runs, not after. Which of the 12 pages lose DDMA is itself unsettled: stripping it from the 6 `fractional-cfo-*` pages and the new `/fractional-cfo` hub is clearly implied by Scott's "DDMA is discoverability work" framing, but `our-services`, `about`, and the 4 vertical pages need his explicit call.
- **Active participation, not "watch and wait" (Scott, 2026-07-20).** The passive "the CFO sits in the room while you decide" is retired everywhere; use active "the CFO works those decisions alongside you" / "is in the decisions with you". Canonical-facts.md § Brand identity carries the master line (amended 2026-07-20). Physical-presence ("work that benefits from being in the room") and structural metaphor ("A CFO in the room", "Where You Sit With Us") are legitimate and stay.

Before editing copy on any checker's say-so, verify the finding against `~/MSIQ/canonical-facts.md` yourself — read the FULL change history for the item (renames get reverted; rules get amended).

- **Bust the CDN cache before calling a deploy wrong (2026-08-03).** A plain `curl` of a
  just-pushed asset measures the CDN edge, not the deploy. The Q2 CC report PDF read as md5
  `2984e765…` / 46285 bytes against a repo file of `9a98dc9d…` / 48354 bytes, which looks like a
  failed publish; the Pages build for that exact commit had already succeeded and a refetch with
  `?cb=<sha>` returned correct parity immediately. Also note `mainstreetiq.com` 301s to `www.`,
  so a `curl` without `-L` measures a redirect stub and its md5 is meaningless. Verify with
  `curl -sL "<url>?cb=$(git rev-parse --short HEAD)"` plus `gh api repos/MainStreetIQ/mainstreetiq-site/actions/runs`
  to confirm the build ran. Only call a deploy wrong when a cache-busted fetch of a
  successfully-built commit still mismatches.
